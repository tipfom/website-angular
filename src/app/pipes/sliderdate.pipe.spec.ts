import { SliderdatePipe } from './sliderdate.pipe';

describe('SliderdatePipe', () => {
  it('create an instance', () => {
    const pipe = new SliderdatePipe();
    expect(pipe).toBeTruthy();
  });
});
