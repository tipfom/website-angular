import { LocaledeltaPipe } from './localedelta.pipe';

describe('LocaledeltaPipe', () => {
  it('create an instance', () => {
    const pipe = new LocaledeltaPipe();
    expect(pipe).toBeTruthy();
  });
});
